@model IEnumerable<onlineSite.Models.product>
@using onlineSite.Models;

@{
    ViewBag.Title = "productView";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>productView</h2>

<div class="row special-list">
    @foreach(product item in ViewBag.lstproduct)
  
    {

        <div class="col-lg-3 col-md-6 special-grid best-seller">
            <div class="products-single fix">
                <div class="box-img-hover">
                    <div class="type-lb">
                        <p class="sale">Sale</p>
                    </div>
                    <img src="../Image/@Url.Content(item.p_img)" width="100px" height="200px" class="img-fluid" alt="Image">
                    <div class="mask-icon">
                        <ul>
                            <li><a href="#" data-toggle="tooltip" data-placement="right" title="View"><i class="fas fa-eye"></i></a></li>
                            <li><a href="#" data-toggle="tooltip" data-placement="right" title="Compare"><i class="fas fa-sync-alt"></i></a></li>
                            <li><a href="#" data-toggle="tooltip" data-placement="right" title="Add to Wishlist"><i class="far fa-heart"></i></a></li>
                        </ul>
                        @*@Html.ActionLink("Add", "addtocart", new { id = item.p_id })*@
                        @*@using (Html.BeginForm("addtocart", "online", new { productid = item.p_id }, FormMethod.Post))*@
                        {


                    <a class="cart" href="#"></a><input type="submit" class="cart" style="color:black; border:none; background-color:red"  value="Add to Cart">
                        }
                    </div>
                </div>
                <div class="why-text">
                    <h4>  @Html.DisplayFor(modelItem => item.p_name)</h4>
                    <h5>   Price : @Html.DisplayFor(modelItem => item.product_price)</h5>
                </div>
            </div>
        </div>
    }
</div>
